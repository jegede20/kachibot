# deps / build / runtime data
node_modules/
dist/
data/
*.log

# secrets — NEVER commit
.env
.env.local

# local junk / personal uploads
uploads/
.cache/
.npm/
*.tar.gz
kachibot-*.png

# stray system marker
.sudo_as_admin_successful

# generated previews
out/

# the brand mark ships with the app (kachibot-*.png above is for local previews)
!assets/kachibot-mark.png
